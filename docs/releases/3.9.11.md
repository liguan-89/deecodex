## 3.9.11

- 应用内更新支持强制策略：启动后自动检查 `latest.json`，命中 `force_update` 时弹出不可关闭的关键更新弹窗，自动下载安装并重启 DEX AI，不再允许继续使用旧版本。
- 发布脚本支持 `DEX_AI_FORCE_UPDATE` / `DEX_AI_FORCE_UPDATE_REASON` / `DEX_AI_MIN_SUPPORTED_VERSION` 三个环境变量，给严重 bug / 安全 / 路由或会话兼容问题保留紧急下发的通道（普通版本不开启）。