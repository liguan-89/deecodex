## 3.9.10

- prune Codex Desktop 临时工作区：把 `~/Documents/Codex/<YYYY-MM-DD>/<对话名>/` 这类 Codex 自动生成的临时目录也识别为 session dir，在 `prune_invalid_desktop_project_state` 时从 `electron-saved-workspace-roots` / `thread-project-assignments` / `sidebar-project-thread-orders` 中清除，避免项目列表被临时对话污染。
